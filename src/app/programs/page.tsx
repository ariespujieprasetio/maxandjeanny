import type {Metadata} from "next"; import {PageHero,SectionHeading,Button} from "@/components/ui"; import {ProgramCard} from "@/components/cards"; import {programs} from "@/data/content";
export const metadata:Metadata={title:"Music Programs",description:"Explore music learning programs at Max & Jeanny."};
export default function Programs(){return <><PageHero eyebrow="Music programs" title="A program for every musical beginning." copy="First instrument, next challenge, or a fresh start—find a way into music that feels right for you."/><section className="page-section"><div className="container"><div className="program-grid">{programs.map(p=><ProgramCard key={p.slug} program={p}/>)}</div></div></section><section className="faq"><div className="container about-split"><SectionHeading eyebrow="Choosing a program" title="Start with curiosity. We’ll help with the rest."/><div className="intro-copy"><p>Think about the learner’s age, interests, and the sounds they are drawn to. A trial class is an easy way to meet an instrument and discover a comfortable place to begin.</p><Button href="/trial">Find your trial class</Button></div></div></section></>}