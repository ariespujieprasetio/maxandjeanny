import type {Metadata} from "next"; import {PageHero} from "@/components/ui"; import {TrialForm} from "@/components/forms"; import {Check} from "lucide-react";
export const metadata:Metadata={title:"Book a Trial Class",description:"Request a trial music class at Max & Jeanny."};
export default async function Trial({searchParams}:PageProps<"/trial">){const query=await searchParams;const age=typeof query.age==="string"?query.age:"";const program=typeof query.program==="string"?query.program:"";const location=typeof query.location==="string"?query.location:"";return <><PageHero eyebrow="Trial class" title="Your first note starts here." copy="Tell us a little about the learner and the music they would love to make. We’ll help you find a comfortable place to begin."/><section className="page-section"><div className="container trial-layout"><aside className="trial-aside"><h2>One simple step.</h2><p>Share your preferences so the right location can help you explore a suitable class.</p>{["Choose a program","Select a preferred location","Share a convenient schedule"].map(x=><p className="checkline" key={x}><Check size={17}/>{x}</p>)}</aside><div className="form-card"><TrialForm initialAge={age} initialProgram={program} initialLocation={location}/></div></div></section></>}
