import type {Metadata} from "next"; import {PageHero,Button} from "@/components/ui"; import {ContactForm} from "@/components/forms"; import {locations} from "@/data/locations";
export const metadata:Metadata={title:"Contact",description:"Contact Max & Jeanny about music lessons, instruments, service, rental, and locations."};
export default async function Contact({searchParams}:PageProps<"/contact">){const query=await searchParams;const branch=typeof query.branch==="string"?query.branch:"";const topic=typeof query.topic==="string"?query.topic:"general";return <><PageHero eyebrow="Contact us" title="Let’s talk about your next step in music." copy="Ask about music lessons, instruments, service, rental, or the best place to begin."/><section className="page-section"><div className="container contact-layout"><aside className="contact-aside"><h2>How can we help?</h2><p>Send an inquiry or speak directly with one of our locations.</p><div className="branch-list">{locations.map(l=><p key={l.id}><strong>{l.shortName}</strong><br/><a href={`tel:${l.phone.replace(/[^0-9+]/g,"")}`}>{l.phone}</a></p>)}</div><Button href="/trial" variant="secondary">Request a trial instead</Button></aside><div className="form-card"><ContactForm initialBranch={branch} initialTopic={topic}/></div></div></section></>}
