import type {MetadataRoute} from "next"; import {locations} from "@/data/locations"; export default function sitemap():MetadataRoute.Sitemap{const routes=["","/programs","/music-store","/services","/locations","/about","/contact","/trial",...locations.map(l=>`/locations/${l.id}`)];return routes.map(path=>({url:`https://example.com${path}`,lastModified:new Date()}))}
