import type {Metadata} from "next"; import {PageHero,Button} from "@/components/ui"; import {LocationCard} from "@/components/cards"; import {locations} from "@/data/content";
export const metadata:Metadata={title:"Locations",description:"Find a Max & Jeanny music school location."};
export default function Locations(){return <><PageHero eyebrow="Our locations" title="Find music in your neighbourhood." copy="Choose a branch that fits naturally into your week, then reach out to explore programs and class times."/><section className="page-section"><div className="container"><div className="location-grid directory">{locations.map(l=><LocationCard key={l.name} location={l}/>)}</div></div></section><section className="trial-banner"><div className="container"><h2>Not sure which branch feels right?</h2><Button href="/contact" variant="light">Ask our team</Button></div></section></>}