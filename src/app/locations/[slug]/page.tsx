import type {Metadata} from "next"; import {notFound} from "next/navigation"; import {MapPin,Phone} from "lucide-react"; import {Button,Eyebrow} from "@/components/ui"; import {locations,getLocation} from "@/data/locations";
export function generateStaticParams(){return locations.map(location=>({slug:location.id}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const branch=getLocation(slug);return branch?{title:branch.name,description:`Visit ${branch.name} in ${branch.city}.`}:{}}
export default async function BranchPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const branch=getLocation(slug);if(!branch)notFound();return <><section className="branch-hero"><div className="container"><Eyebrow>Max & Jeanny location</Eyebrow><h1>{branch.name}</h1><p><MapPin/>{branch.address}, {branch.city} {branch.postalCode}</p><p><Phone/>{branch.phone}</p><div className="hero-actions"><a className="button button-primary" href={`tel:${branch.phone.replace(/[^0-9+]/g,"")}`}>Call Branch</a><a className="button button-secondary" href={branch.mapsUrl} target="_blank" rel="noreferrer">View on Maps</a></div></div></section><section><div className="container about-split"><div><Eyebrow>At this location</Eyebrow><h2 className="branch-heading">Your music journey, close to home.</h2></div><div><div className="service-tags">{branch.services.map(service=><span key={service}>{service}</span>)}</div><p>Ask the branch about current programs, instruments, and service availability.</p><Button href={`/trial?location=${branch.id}`}>Book a Trial Class</Button></div></div></section></>}
