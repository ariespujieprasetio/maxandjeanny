import Image from "next/image"; import Link from "next/link"; import {ArrowRight,MapPin} from "lucide-react"; import type {Program} from "@/data/content";
export function ProgramCard({program}:{program:Program}){return <article className="program-card"><div className="card-image"><Image src={program.image} alt={`${program.title} lesson`} fill sizes="(max-width:768px) 100vw, 33vw"/></div><div className="program-body"><span>{program.audience}</span><h3>{program.title}</h3><p>{program.description}</p><Link href={`/trial?program=${program.slug}`}>Explore {program.title} <ArrowRight size={17}/></Link></div></article>}
export function LocationCard({location}:{location:{name:string;city:string;address:null;phone:null;verified:boolean}}){return <article className="location-card"><div className="pin"><MapPin size={20}/></div><span>{location.city}</span><h3>{location.name}</h3><div className="location-actions"><Link href="/locations">View Branch</Link><Link href={`/contact?branch=${encodeURIComponent(location.name)}`}>Contact Branch <ArrowRight size={15}/></Link></div></article>}
