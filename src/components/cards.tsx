import Image from "next/image"; import Link from "next/link"; import {ArrowRight,MapPin,Phone} from "lucide-react"; import type {Program} from "@/data/programs"; import type {Branch} from "@/data/locations";
export function ProgramCard({program}:{program:Program}){return <article className="program-card"><div className="card-image"><Image src={program.image} alt={`${program.title} lesson`} fill sizes="(max-width:768px) 100vw, 33vw"/></div><div className="program-body"><span>{program.audience}</span><h3>{program.title}</h3><p>{program.description}</p><Link href={`/trial?program=${program.slug}`}>Explore {program.title} <ArrowRight size={17}/></Link></div></article>}
export function LocationCard({location}:{location:Branch}){return <article className={`location-card${location.primary?" primary-location":""}`}><div className="pin"><MapPin size={20}/></div><span>{location.city}</span><h3>{location.shortName}</h3><p>{location.address}, {location.city} {location.postalCode}</p><a className="branch-phone" href={`tel:${location.phone.replace(/[^0-9+]/g,"")}`}><Phone size={15}/>{location.phone}</a><div className="location-actions"><Link href={`/locations/${location.id}`}>View Branch</Link><a href={location.mapsUrl} target="_blank" rel="noreferrer">View on Maps <ArrowRight size={15}/></a></div></article>}
