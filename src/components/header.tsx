"use client"; import Link from "next/link"; import {useEffect,useState} from "react"; import {Menu,X} from "lucide-react"; import {siteConfig} from "@/config/site";
export function Brand(){return <Link href="/" className="brand" aria-label="Max & Jeanny home"><strong>{siteConfig.brand}</strong><span>{siteConfig.descriptor}</span></Link>}
export function SiteHeader(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>24);onScroll();window.addEventListener("scroll",onScroll,{passive:true});return()=>window.removeEventListener("scroll",onScroll)},[]);return <header className={`site-header${scrolled?" scrolled":""}`}><div className="container nav-wrap"><Brand/><nav className={open?"nav open":"nav"} aria-label="Primary navigation">{siteConfig.navigation.map(n=><Link key={n.href} href={n.href} onClick={()=>setOpen(false)}>{n.label}</Link>)}<Link className="mobile-trial" href="/trial" onClick={()=>setOpen(false)}>Book a Trial Class</Link></nav><Link className="header-cta" href="/trial">Book a Trial Class</Link><button className="menu-button" onClick={()=>setOpen(!open)} aria-label="Toggle navigation" aria-expanded={open}>{open?<X/>:<Menu/>}</button></div></header>}
