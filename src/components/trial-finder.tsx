"use client"; import {useRouter} from "next/navigation"; import {useState,type FormEvent} from "react"; import {ArrowRight} from "lucide-react"; import {programs,locations} from "@/data/content";
export function TrialFinder(){const router=useRouter();const[age,setAge]=useState("");const[program,setProgram]=useState("");const[location,setLocation]=useState("");function go(e:FormEvent){e.preventDefault();const q=new URLSearchParams({age,program,location});router.push(`/trial?${q}`)}return <form className="trial-finder" onSubmit={go}><label><span>Student age</span><input required type="number" min="2" max="100" value={age} onChange={e=>setAge(e.target.value)} placeholder="Age"/></label><label><span>Interested program</span><select required value={program} onChange={e=>setProgram(e.target.value)}><option value="" disabled>Choose program</option>{programs.map(p=><option key={p.slug} value={p.slug}>{p.title}</option>)}</select></label><label><span>Preferred location</span><select required value={location} onChange={e=>setLocation(e.target.value)}><option value="" disabled>Choose branch</option>{locations.map(l=><option key={l.name}>{l.name}</option>)}</select></label><button type="submit">Find a Trial Class <ArrowRight size={18}/></button></form>}
